Read [AGENTS.md](../AGENTS.md) before making changes. Follow conventional commits. Run `pnpm check && pnpm test && pnpm build` before finishing.

Implement widget UI in `src/components/WidgetPanel.vue`. Use `useWidgetConfig()` for `apiBase` and other options.
